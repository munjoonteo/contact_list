# Contact List

A simple contact list app made using React which gets information from [jsonplaceholder](https://jsonplaceholder.typicode.com/users).

To run the app, run the command `npm start` from the root of the directory.

Please try it in both mobile and desktop!
