# Contact List

A simple contact list app which gets information from [jsonplaceholder](https://jsonplaceholder.typicode.com/users).

Made using React.

To run the app, run the command `npm start` from the root of the directory.
